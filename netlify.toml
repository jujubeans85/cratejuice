[build]
  command = ""
  publish = "v3/frontend"

# Proxy frontend /api/* → Render backend
[[redirects]]
  from = "/api/*"
  to = "https://cratejuice.onrender.com/:splat"
  status = 200
  force = true
  headers = { Access-Control-Allow-Origin = "*" }

# SPA fallback (optional; safe to keep)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
