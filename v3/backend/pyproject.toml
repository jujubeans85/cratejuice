[project]
name = "cratejuice-backend"
version = "0.0.1"
description = "CrateJuice backend (FastAPI)"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
