# CrateJuice v3 Backend (FastAPI)

Routes
------
- `GET /health` → `{"ok": true}`

Local run
---------
```bash
pip install --upgrade pip && pip install -r requirements.txt
uvicorn main:app --reload
```
