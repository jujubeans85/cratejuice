# Netlify redirects: proxy CJ API
[[redirects]]
  from = "/api/*"
  to = "https://RENDER-BASE.onrender.com/:splat"
  status = 200
  force = true

# Optional: SPA fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200